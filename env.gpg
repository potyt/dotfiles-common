#! /usr/bin/env sh

# this file should be sourced, not run

[ -f "$GPG_AGENT_ENV_FILE" ] || return

. $GPG_AGENT_ENV_FILE
export GPG_AGENT_INFO
export GPG_AUTH_SOCK=$(echo $GPG_AGENT_INFO | cut -d: -f 1)
export GPG_AGENT_PID=$(echo $GPG_AGENT_INFO | cut -d: -f 2)
