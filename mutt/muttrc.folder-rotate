push ":set my_f = \`ls \$MAILDIR | grep -v archive | sed '$my_fx!d' | xargs -n 1 basename\`<enter>"
push ":set my_fx = \`echo $(( $my_fx % $(ls \$MAILDIR | grep -v archive | wc -l) + 1 ))\`<enter>"
source $my_conf/muttrc.folder-load
